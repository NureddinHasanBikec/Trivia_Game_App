import React from 'react';
import {
    View,
    Text,
    SafeAreaView,
} from 'react-native';

const Finish = (props) => {
    return(
        <SafeAreaView style={{flex: 1}}>
            <View style={{flex: 1}}>
                <Text>Finish</Text>
            </View>
        </SafeAreaView>
    );
};

export {Finish};